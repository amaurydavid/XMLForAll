{
  "name": "XMLForAll",
  "version": "0.1.0",
  "description": "Generates XML styleguide on all project types.",
  "scripts": {
    "start": "zem start",
    "build": "zem build",
    "clean": "zem clean",
    "exec": "zem exec",
    "publish": "zem publish"
  },
  "zeplin": {
    "displayName": "XMLForAll",
    "projectTypes": [
      "android",
      "ios",
      "osx",
      "web"
    ]
  },
  "dependencies": {
    "zem": "^0.3.3"
  }
}
